image: node:8.12.0

cache:
  paths:
   - node_modules/

before_script:
  - npm install

stages:
  - build_deploy

build_&_deploy_app:
  stage: build_deploy
  only:
   - master
  script:
   - node_modules/gulp/bin/gulp.js --gulpfile ./gulpfile.js build
  artifacts:
    paths:
      - public/
